{
    "build": {
      "env": {
        "DJANGO_SETTINGS_MODULE": "web_agent.settings"
      },
      "cmd": "pip install -r requirements.txt && python manage.py tailwind build && python manage.py collectstatic --noinput"
    },
    "routes": [
      { "src": "/static/(.*)", "dest": "/static/$1" },
      { "src": "/(.*)", "dest": "index.html" }
    ]
  }
  